


// get/set webservices
// string webserviceName : 	the name of the webservice we want to contact
// object args			 : 	the object of arguments to pass to the webservice
// list of webservices :
//     "get_best_submission"

webservice = function (webserviceName, args) {
	$.ajax({
		data: args,
		succes: function(obj) {
			return obj;
		}
	})
}
//todo améliorer ça (ébauche)